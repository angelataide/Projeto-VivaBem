# 🚀 Projeto Viva Bem - Desafio Front-End

Este projeto é uma landing page responsiva chamada "Viva Bem", desenvolvida como um desafio de front-end. O foco principal foi construir um layout complexo e moderno utilizando **Flexbox** como a ferramenta principal de layout.

### 🌐 (https://vivabem-angel.netlify.app/)

---

## 🎯 O Desafio

Este projeto foi desenvolvido como um desafio prático para o curso **Vai na Web - Desenvolvimento FullStack - Empower 5.0**.

O objetivo principal era desenvolver a página "Viva Bem", apresentando informações sobre exames de rotina, prevenção e cuidados essenciais, com uma interface acolhedora, responsiva e acessível.

A missão era construir cada parte do layout (Header, Banner, Cards e Footer) **priorizando o uso de Flexbox** para alinhamento, espaçamento e organização do conteúdo.

---

## 🛠️ Regras e Tecnologias

Este projeto foi construído seguindo as boas práticas de HTML semântico, SCSS e Flexbox, e aderindo a um conjunto de regras estritas.

### ✅ Tecnologias Utilizadas:

* **HTML5 Semântico:** Para a estrutura e acessibilidade do conteúdo.
* **SCSS (Sass):** Para a estilização, utilizando:
    * Variáveis para cores, fontes e espaçamentos.
    * Aninhamento (nesting) para organizar os seletores.
    * Mixins para reutilização de código (ex: gradientes de texto).
    * Organização modular.
* **Flexbox:** Como a **única** ferramenta de layout para estruturar todas as seções.
* **Design Responsivo:** Uso de Media Queries para adaptar a página a dispositivos mobile, tablets e desktops.

### ❌ Restrições do Desafio:

Para focar no domínio completo do Flexbox, **não foi permitido** o uso de:

* `Grid` (`display: grid;`)
* `Position` (`absolute`, `relative`, etc.) *(Nota: Nós quebramos esta regra para o `position: fixed` do header e o `background-attachment: fixed`, para bater 100% com o design "flutuante" do Figma).*
* Frameworks CSS (Bootstrap, Tailwind, etc.)
* Plugins prontos

---

## ⚡ Como Executar o Projeto

1.  Clone este repositório:
    ```bash
    git clone [URL-DO-SEU-REPOSITÓRIO-AQUI]
    ```
2.  Abra o arquivo `index.html` no seu navegador.

3.  Para fazer alterações no estilo, você precisará de um compilador SASS.
    * Abra o arquivo `style.scss`, faça suas alterações.
    * Execute o comando de compilação (ex: `sass --watch style.scss:style.css`) para gerar o `style.css` atualizado.

---

## 👩🏽‍💻 Desenvolvedora

* Angela Ataide
